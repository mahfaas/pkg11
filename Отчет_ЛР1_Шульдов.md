# Отчёт по лабораторной работе №1
**Дисциплина:** Практикум по компьютерной графике  
**Студент:** Шульдов Станислав, группа 11  
**Тема:** Цветовые модели (RGB, CMYK, HSV, HLS, XYZ, LAB)  

---

## 1. Цель работы

Изучить цветовые модели RGB, CMYK, HSV, HLS, XYZ, LAB и алгоритмы перехода между ними. Разработать интерактивное веб-приложение, отображающее цвет одновременно в нескольких моделях с возможностью редактирования из любой из них.

---

## 2. Задача и реализованные требования

- Задание цвета тремя способами для каждой модели: ползунки, числовые поля, графическая палитра.
- Автоматический пересчёт всех моделей при изменении любой компоненты.
- Все расчёты выполняются вручную, без сторонних библиотек конвертации.
- Динамические матрицы RGB ↔ XYZ — без жёстких констант. Поддержка стандартов освещения D65, D50, E с пересчётом матрицы на лету.
- Алгоритмы цветоделения CMYK: GCR и UCR с регулировкой силы применения.
- Стратегии обработки выхода за цветовой охват: Clipping и Scaling.
- Динамические CSS-градиенты на ползунках: фон ползунка показывает цвета, доступные при его сдвиге.
- Ненавязчивое предупреждение при выходе цвета за допустимый диапазон.
- Архитектура MVC: Model (математика), View (DOM), Controller (связка).
- Автоматические микро-тесты математических функций при загрузке страницы.

---

## 3. Инструментарий

| Компонент | Описание |
|---|---|
| Язык | JavaScript (ES5/ES6), без сторонних библиотек |
| Платформа | Браузер, GitHub Pages |
| Разметка и стили | HTML5, CSS3 |
| Архитектура | MVC (Model–View–Controller) |

---

## 4. Ход работы

### 4.1 Архитектура приложения (MVC)

Код разделён на три независимых файла:

- **`js/model.js` — Model:** вся математика преобразований. Хранит текущий цвет в линейном RGB. Не знает ничего про DOM.
- **`js/view.js` — View:** управление DOM. Строит карточки моделей, генерирует CSS-градиенты ползунков. Не содержит формул.
- **`js/controller.js` — Controller:** слушает события из View, передаёт данные в Model, инициирует перерисовку.
- **`js/tests.js`:** автоматические тесты при загрузке страницы.

### 4.2 Реализованные преобразования

**RGB ↔ XYZ**  
Матрица перехода строится динамически по методу Брюса Линдблума из хроматичностей праймериз sRGB и координат выбранного опорного белого (D65, D50 или E). При смене источника освещения матрица пересчитывается на лету. Применяется гамма-коррекция sRGB.

**XYZ ↔ LAB**  
Стандартные формулы CIE 1976. Координаты белого (Xn, Yn, Zn) также зависят от выбранного стандарта освещения:

```
f(t) = t > (6/29)³ ? cbrt(t) : t/(3·(6/29)²) + 4/29
L = 116·f(Y/Yn) − 16
a = 500·(f(X/Xn) − f(Y/Yn))
b = 200·(f(Y/Yn) − f(Z/Zn))
```

**RGB ↔ CMYK**  
Реализованы оба алгоритма цветоделения:
- **GCR:** `K = min(C₀,M₀,Y₀)·amount; C = (C₀−K)/(1−K)` — серая составляющая заменяется чёрным по всему диапазону.
- **UCR:** `K = min(C₀,M₀,Y₀)·amount; C = C₀−K` — замена только в глубоких тенях.

**RGB ↔ HSV и RGB ↔ HLS**  
Секторные формулы по максимальному/минимальному каналу нормированного RGB ∈ [0, 1].

### 4.3 Обработка выхода за цветовой охват

При XYZ/LAB→RGB, если значения выходят за [0, 1]:

- **Clipping:** `v' = clamp(v, 0, 1)` — жёсткое прижатие к границам.
- **Scaling:** `v' = (v − lo) / (hi − lo)` — пропорциональное масштабирование с сохранением тона.

При выходе за гамму пользователь видит предупреждение в интерфейсе.

### 4.4 Интерфейс

Задание цвета тремя способами для каждой из шести моделей:
1. **Ползунок** с динамическим CSS-градиентом (показывает, какой цвет получится при сдвиге).
2. **Числовое поле** для точного ввода.
3. **Графическая палитра:** HSV-квадрат + полоса тона + нативный Color Picker.

### 4.5 Автоматические тесты

При загрузке `tests.js` прогоняет эталонные значения через функции `model.js`:

| Вход | Ожидаемый результат |
|---|---|
| RGB(255, 0, 0), D65 → LAB | L=53.24, a=80.09, b=67.20 (±0.5) |
| RGB(255, 0, 0), D65 → CMYK | C=0, M=100, Y=100, K=0 (±0.1) |
| RGB(255, 255, 255) → LAB | L=100, a=0, b=0 (±0.1) |

Результат отображается прямо в интерфейсе и в консоли браузера (F12).

---

## 5. Вывод

В ходе работы разработано интерактивное веб-приложение с архитектурой MVC для конвертации цветов между шестью моделями (RGB, XYZ, LAB, CMYK, HSV, HLS). Все расчёты выполняются математически, без сторонних библиотек. Реализованы динамические матрицы перехода, два алгоритма цветоделения CMYK, две стратегии гамут-мэппинга и автоматические тесты.

---

**Ссылка на проект:** https://mahfaas.github.io/pkg11/  
**Исходный код:** https://github.com/mahfaas/pkg11
